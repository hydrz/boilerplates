version: '3'

networks:
  default:
    external: true
    name: br0

services:
  mongo:
    image: bitnami/mongodb:5.0.7
    container_name: mongo
    restart: always
    environment:
      MONGODB_ROOT_PASSWORD: root
    volumes:
      - ./mongo:/bitnami/mongodb
